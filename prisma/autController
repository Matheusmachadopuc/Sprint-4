import usuarioSchema from  "../models/usuarioSchema.js"; // copia de models
import bcrypt from "bcrypt"; //para criptografar a senha 
import jwt from "jsonwebtoken"; //autenticação e autorização. 

import * as dotenv from "dotenv"; //permite carregar variaveis dentro de um arquivo env. p/app Node.js 

dotenv.config ();

const SECRET: string = process.env.SECRET as string;

